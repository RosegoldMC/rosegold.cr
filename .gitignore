/docs/
/lib/
/bin/
/.shards/
*.dwarf
*.env*
.spectator-failures

# binaries
/play
/rosegold

# Libraries don't need dependency lock
# Dependencies will be locked in applications that use them
/shard.lock

# Developer-local dependency overrides (e.g. minecraft-data from a local path)
/shard.override.yml

# whole entire minecraft server for specs
spec/fixtures/server/*
!spec/fixtures/server/ops.json

# added by copilot
crystal.tar.gz

tmp

# User-specific Claude Code settings
.claude/settings.local.json
